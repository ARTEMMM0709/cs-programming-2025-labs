# Создаем список
numbers = [12, 45, 23, 67, 34]

# Находим максимальное число
max_num = max(numbers)
# Находим длину списка
length = len(numbers)
# Делим максимальное на длину
result = max_num / length

print(result)